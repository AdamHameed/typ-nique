# Web service example for production documentation.
# Only NEXT_PUBLIC_* values are exposed to the browser bundle.

NODE_ENV=production
PORT=3000
API_INTERNAL_URL=http://api:4000
# Optional. Leave empty if browser REST traffic should use the same-origin web proxy.
NEXT_PUBLIC_API_URL=
# Recommended when browser WebSocket traffic should go directly to the public API service.
NEXT_PUBLIC_MULTIPLAYER_GATEWAY_URL=https://api.example.com
NEXT_PUBLIC_ENABLE_MULTIPLAYER_DIAGNOSTICS=false
